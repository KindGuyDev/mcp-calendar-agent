{
  "name": "event-calendar-mcp",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Custom MCP server backing the /event workflow's calendar with a local JSON file store.",
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "start": "node dist/index.js",
    "dev": "tsx src/index.ts",
    "test": "npm run build && tsx --test test/store.test.ts test/server.integration.test.ts"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0",
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "@types/node": "^22.20.2",
    "tsx": "^4.19.0",
    "typescript": "^5.7.0"
  }
}
